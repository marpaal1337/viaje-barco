# No se requieren API keys.
# Usa Playwright para scraping automático:
#   pip install playwright && playwright install chromium
#   python scraper_vuelos.py --auto
#
# O modo manual:
#   python scraper_vuelos.py --urls    (genera enlaces Google Flights)
#   python scraper_vuelos.py --editar  (edita precios)
